#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	int n;
	scanf("%d", &n);

	for (int i = 1;i <= n;i++) {
		for (int k = 1;k < i ;k++) {
			printf(" ");
		}

		for (int j = 2*(n-i+1)-1;j >= 1;j--) {
			printf("*");
		}
	printf("\n");
	}
}
