#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	int a,b,c,d,e;
	double avg = 0;

	scanf("%d %d %d %d %d", &a,&b,&c,&d,&e);

	avg = (a + b + c + d + e) / 5.0;

	if (avg >= 85) {
		printf("A\n");
	}

	else if (avg >= 70) {
		printf("B\n");
	}

	else {
		printf("F\n");
	}

	printf("%.2lf", avg);
}
-----------------------------------------
#include <stdio.h>
#pragma warning (disable : 4996)

int main() {
	int y1, m1, d1, y2, m2, d2, y3, m3, d3;

	scanf("%d/%d/%d %d/%d/%d %d/%d/%d", &y1, &m1, &d1, &y2, &m2, &d2, &y3, &m3, &d3);

	int yr = y1, mr = m1, dr = d1;
	
	if (y2 < yr) {
		yr = y2;
		mr = m2;
		dr = d2;
	}

	else if (y2 == yr) {
		if (m2 < mr) {
			mr = m2;
			dr = d2;
		}

		else if (m2 == mr) {
			if (d2 < dr) {
				dr = d2;
			}
		}
	}


	if (y3 < yr) {
		yr = y3;
		mr = m3;
		dr = d3;
	}

	else if (y3 == yr) {
		if (m3 < mr) {
			mr = m3;
			dr = d3;
		}

		else if (m3 == mr) {
			if (d3 < dr) {
				dr = d3;
			}
		}
	}

	printf("%d/%02d/%02d", yr,mr,dr);

}
-----------------------------------------
#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	int n;

	scanf("%d", &n);

	if (n % 2 == 0 && n % 3 == 0 && n % 5 == 0) {
		printf("A");
	}

	else if (n % 2 == 0 && n % 3 == 0 && n % 5 != 0 || n % 2 == 0 && n % 3 != 0 && n % 5 == 0 || n % 2 != 0 &&
		n % 3 == 0 & n % 5 == 0) {
		printf("B");
	}

	else if (n % 2 == 0 && n % 3 != 0 && n % 5 != 0 || n % 2 != 0 && n % 3 == 0 && n % 5 != 0 || n % 2 != 0 &&
		n % 3 != 0 & n % 5 == 0) {
		printf("C");
	}

	else {
		printf("D");
	}

}
----------------------------------------
#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	int n;
	scanf("%d", &n);

	if (n % 2 == 0 && n % 3 == 0 && n % 5 == 0) {
		printf("%d %d %d", 2,3,5);
	}

	else if (n % 2 == 0 && n % 3 == 0 && n % 5 != 0) {
		printf("%d %d", 2,3);
	}

	else if (n % 2 == 0 && n % 3 != 0 && n % 5 == 0) {
		printf("%d %d", 2, 5);
	}

	else if (n % 2 != 0 && n % 3 == 0 && n % 5 == 0) {
		printf("%d %d", 3,5);
	}

	else if (n % 2 == 0 && n % 3 != 0 && n % 5 != 0) {
		printf("%d" ,2);
	}

	else if (n % 2 != 0 && n % 3 == 0 && n % 5 != 0) {
		printf("%d", 3);
	}

	else if (n % 2 != 0 && n % 3 != 0 && n % 5 == 0) {
		printf("%d", 5);
	}

	else {
		printf("N");
	}
}
--------------------------------------------------------
#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	char s;
	int n;
	scanf("%c %d", &s, &n);

	if (s >= 'a' && s <= 'z') {
		if (n + s > 'z') {
			n = (n + s) % 'z' - 1;
			printf("%c", 'a' + n);
		}
		
		else {
			printf("%c", n + s);
		}
	}

	else if (s >= 'A' && s <= 'Z') {
		if (n + s > 'Z') {
			n = (n + s) % 'Z' - 1;
			printf("%c", 'A' + n);
		}

		else {
			printf("%c", n + s);
		}
	}

	else {
		printf("%c", s);
	}
}
---------------------------------------
#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	int a,b,c,sum = 0,cnt = 0, is100 = 0;
	double avg;

	scanf("%d %d %d", &a,&b,&c);

	if (a < 70) {
		cnt += 1;
	}

	if (b < 70) {
		cnt += 1;
	}

	if (c < 70) {
		cnt += 1;
	}

	if (a == 100 || b == 100 || c == 100) {
		is100 = 1;
	}

	avg = sum / 3.0;

	if (cnt >= 2) {
		if (is100 == 0) {
			printf("F\n");
			printf("%.2lf", avg);
		}
		else {
			if (avg >= 85) {
				printf("A\n");
				printf("%.2lf", avg);
			}
			else if (avg >= 70) {
				printf("B\n");
				printf("%.2lf", avg);
			}
			else {
				printf("F\n");
				printf("%.2lf", avg);
			}
		}
	}
	else {
		if (avg >= 85) {
			printf("A\n");
			printf("%.2lf", avg);
		}
		else if (avg >= 70) {
			printf("B\n");
			printf("%.2lf", avg);
		}
		else {
			printf("F\n");
			printf("%.2lf", avg);
		}
	}
}	
----------------------------------------------
#include <stdio.h>
#pragma warning (disable : 4996)

int main() {
	char grade = ' ';
	int a,b,c,d,e, sum = 0, cnt_g = 0, cnt_b = 0, max = -1, min = 101;
	double avg;

	scanf("%d %d %d %d %d", &a, &b, &c, &d, &e);

	sum += a;
	if (a == 100) {
		cnt_g += 1;
	}
	if (a < 70) {
		cnt_b += 1;
	}
	if (a >= max) {
		max = a;
	}
	if (a <= min) {
		min = a;
	}

	sum += b;
	if (b == 100) {
		cnt_g += 1;
	}
	if (b < 70) {
		cnt_b += 1;
	}
	if (b >= max) {
		max = b;
	}
	if (a <= min) {
		min = b;
	}

	sum += c;
	if (c == 100) {
		cnt_g += 1;
	}
	if (c < 70) {
		cnt_b += 1;
	}
	if (c >= max) {
		max = c;
	}
	if (c <= min) {
		min = c;
	}

	sum += d;
	if (d == 100) {
		cnt_g += 1;
	}
	if (d < 70) {
		cnt_b += 1;
	}
	if (d >= max) {
		max = d;
	}
	if (d <= min) {
		min = d;
	}

	sum += e;
	if (e == 100) {
		cnt_g += 1;
	}
	if (e < 70) {
		cnt_b += 1;
	}
	if (e >= max) {
		max = e;
	}
	if (e <= min) {
		min = e;
	}

	avg = sum / 5.0;

	if (avg > +85) {
		grade = 'A';
	}
	else if (avg >= 70) {
		grade = 'B';
	}
	else {
		grade = 'F';
	}

	printf("%c %.2lf\n", grade, avg);
	
	if (cnt_g >= 2) {
		printf("Good\n");
	}
	if (avg >= 90.0 && cnt_b == 0) {
		printf("Excellent\n");
	}
	if (cnt_b != 0) {
		printf("Bad\n");
	}
	if (max - min >= 30) {
		printf("Unbalanced");
	}
}
