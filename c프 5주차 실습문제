#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	char a;
	scanf("%c", &a);

	if (a >= 'a' && a <= 'z') {
		printf("%c", a - 'a' + 'A');
	}

	else if (a >= 'A' && a <= 'Z') {
		printf("%c", a - 'A' + 'a');
	}

	else {
		printf("none");
	}
}

-------------------------------------------
#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	char a,b,b1;
	scanf("%c-%c", &a, &b);

	b1 = b - 'a' + 'A'; 

	if (a > b1) {
		printf("%c", b);
	}
	
	else if (a < b1) {
		printf("%c", a);
	}

	else {
		printf("%c", a);
	}
}

----------------------------------------
#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	int a,b,c,d,flag = 0;
	scanf("%d %d %d %d", &a, &b, &c, &d);

	if (a > b) {
		printf("DOWN ");
		flag = 1;
	}

	else if (a < b) {
		printf("UP ");
		flag = 1;
	}

	if (b > c) {
		printf("DOWN ");
		flag = 1;
	}

	else if (b < c) {
		printf("UP ");
		flag = 1;
	}

	if (c > d) {
		printf("DOWN ");
		flag = 1;
	}

	else if (c < d) {
		printf("UP");
		flag = 1;
	}

	if (flag == 0) {
		printf("NONE");
	}
}
--------------------------------------
#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	int n,m;
	scanf("%d", &n);
	scanf("%d", &m);

	if (n == m) {
		printf("RIGHT\n");
	}

	else if (n > m) {
		printf("UP\n");

		scanf("%d", &m);
		if (n > m) {
			printf("UP\n");
		}
		else if (n < m) {
			printf("DOWN\n");
		}
		else {
			printf("RIGHT\n");
		}
	}

	else {
		printf("DOWN\n");

		scanf("%d", &m);
		if (n > m) {
			printf("UP\n");
		}
		else if (n < m) {
			printf("DOWN\n");
		}
		else {
			printf("RIGHT\n");
		}
	}
}
------------------------------------
#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	int n,m;
	scanf("%d", &n);
	scanf("%d", &m);

	if (n == 1) {
		printf("Americano\n");

		m -= 500;
		printf("%d %d", m / 500, m % 500 / 100);
	}

	else if (n == 2) {
		printf("Cafe Latte\n");

		m -= 400;
		printf("%d %d", m / 500, m % 500 / 100);
	}

	else {
		printf("Lemon Tea\n");

		m -= 300;
		printf("%d %d", m / 500, m % 500 / 100);
	}
}
------------------------------------------
#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	int a,b,c,d,e,sum = 0, cnt = 0, min = 101, flag = 0;
	double avg = 0;

	scanf("%d", &a);
	sum += a;
	if (a < 40) {
		cnt += 1;
	}
	if (a > 100 || a < 0) {
		flag = 1;
	}
	if (a <= min) {
		min = a;
	}

	scanf("%d", &b);
	sum += b;
	if (b < 40) {
		cnt += 1;
	}
	if (b > 100 || b < 0) {
		flag = 1;
	}
	if (b <= min) {
		min = b;
	}

	scanf("%d", &c);
	sum += c;
	if (c < 40) {
		cnt += 1;
	}
	if (c > 100 || c < 0) {
		flag = 1;
	}
	if (c <= min) {
		min = c;
	}

	scanf("%d", &d);
	sum += d;
	if (d < 40) {
		cnt += 1;
	}
	if (d > 100 || d < 0) {
		flag = 1;
	}
	if (d <= min) {
		min = d;
	}

	scanf("%d", &e);
	sum += e;
	if (e < 40) {
		cnt += 1;
	}
	if (e > 100 || e < 0) {
		flag = 1;
	}
	if (e <= min) {
		min = e;
	}

	if (flag == 1) {
		printf("Invalid");
	}
	else {
		avg = sum / 5.0;
		if (cnt >= 3) {
			printf("F\n");
		}

		else {
			if (avg >= 90) {
				printf("A+\n");
			}
			else if (avg >= 75) {
				printf("A\n");
			}
			else if (avg >= 65) {
				printf("B+\n");
			}
			else {
				printf("B\n");
			}
		}

		printf("avg=%.2lf min=%d", avg,min);
	}	
}
--------------------------------------------
#pragma warning (disable : 4996)
#include <stdio.h>

int main() {
	int n,m;
	scanf("%d", &n);
	scanf("%d", &m);

	switch (n) {
		case 1:
			printf("Americano\n");

			m -= 500;
			printf("%d %d", m / 500, m % 500 / 100);
			break;

		case 2:
			printf("Cafe Latte\n");

			m -= 400;
			printf("%d %d", m / 500, m % 500 / 100);
			break;

		case 3:
			printf("Lemon Tea\n");

			m -= 300;
			printf("%d %d", m / 500, m % 500 / 100);
			break;
	}
}
